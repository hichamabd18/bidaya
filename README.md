# اليوم النبوي ووظائف العام

الدليل الشامل للاقتداء والتعبد وتزكية النفس وفق السنن النبوية الشريفة ووظائف مواسم العام ومتتبع العادات الإيمانية.

تطبيق ويب عربي (RTL أولًا) يعمل كـ PWA: مسار اليوم النبوي محطةً محطة، وظائف الشهور الهجرية، مكتبة السنن والأدعية والقواعد، متتبع أوراد يومي/أسبوعي/شهري، مسبحة تفاعلية، ومواقيت صلاة فلكية لكل ولايات الجزائر (58) ومدن إسلامية مختارة — كل ذلك دون خادم أو حساب.

## التشغيل

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # إنتاج (Turbopack)
npm run lint       # ESLint (flat config)
npm run typecheck  # tsc --noEmit
npm test           # اختبارات ذهبية للمواقيت والتقويم
```

## البنية

```
app/                    مسارات App Router (كل صفحة خادمية تعرض مكون عرض عميل)
  page.tsx              → اليوم (المسار الافتراضي)
  seasons/              → وظائف الشهور وفصول العام
  library/              → فهرس المكتبة
  library/[id]/         → صفحات قراءة المقالات (61 صفحة مولّدة ساكنًا)
  progress/             → تقدمي (صحيفة اليوم والأسبوع والسجل)
components/
  providers/AppProvider.tsx   حالة مركزية: الإعدادات، المواقيت، سجلات الإنجاز، النوافذ
  shell/                الترويسة، التنقل، المسبحة، الإعدادات، البحث، التذييل
  today/ seasons/ library/ progress/   شاشات الأقسام
  ui/                   البدائيات: Sheet, Toast, CheckRow/PressRow, controls, ScriptureBlock
lib/
  data/                 المحتوى الموثّق (أنواع، مسار اليوم، مواسم، سياقي، قواعد، عادات)
  prayer.ts             المحرك الفلكي (Fajr 18°/Isha 17°) + حل إزاحة IANA + التقويم الهجري
  store.ts              مخزن موسوم v1 بمفاتيح دورية (يوم/أسبوع/شهر)
  search.ts             تطبيع عربي + بحث
  library.ts            نموذج مقالات المكتبة ومعرّفاتها الثابتة
  native.ts             Wake Lock / Web Share / كشف iOS
  sound.ts              محرك المؤثرات الصوتية والاهتزاز
  useNow.ts             ساعة حية عبر useSyncExternalStore
public/                 manifest + عامل خدمة + أيقونات
scripts/golden.test.mjs اختبارات ذهبية
```

## قرارات هندسية موثّقة

- **مواقيت الصلاة**: فلكية خالصة بزوايا وزارة الشؤون الدينية الجزائرية (الفجر 18°، العشاء 17°، العصر بمقدار ظل الجذر). تُحلّ إزاحة التوقيت لكل تاريخ من معرّف IANA عبر `Intl` — فتتحقق القاهرة والقدس (التوقيت الصيفي) وأي موقع GPS على صواب. **ملاحظة**: القيم بلا «احتياط الوزارة»؛ قد تجد جداول رسمية فجرًا متأخرًا ~10-20 دقيقة بحسب المعايير المحلية.
- **التقويم الهجري**: أُبقيت الخوارزمية الجدولية السابقة عمدًا (لا تتغير التواريخ الشرعية المعروضة) مع معايرة الرؤية ±2 يوم في الإعدادات. بديل المنصة `Intl islamic-umalqura` متاح عند الحاجة.
- **الاستمرارية**: `bidaya.v1:*` — سجلات مفاتيحها دورية (`day:2026-09-25`، `week:2026-W39`، `month:…`) فلا يُمحى تقدّم الأسبوع بانتهاء اليوم، مع أرشيف 30 يومًا في «تقدمي» وقصّ تلقائي بعد 120 يومًا.
- **النسخة الاحتياطية**: تصدير/استعادة ملف JSON من الإعدادات (`lib/backup.ts`) — الاستعادة «تُكمل ما ينقص» فلا تحذف علامات أحدث، وتتحقق من صحة المفاتيح والملف قبل الكتابة. بديل بلا خادم لحماية السجل طويل الأمد؛ إن طُلبت مزامنة بين الأجهزة فطبقة Supabase local-first تتغلّف على نفس المفاتيح.
- **العزلة الزمنية**: نبضة الدقيقة في الموفّر لكل الحسابات، وساعة الثانية عبر `useSyncExternalStore` تُحدّث العدّاد فقط — لا تصيير كامل كل ثانية.
- **المظهر**: آلية واحدة (`data-theme`) تُطبَّع قبل الترطيب (لا وميض أبيض)، و«فجر»: لون الصفحة يتبع محطة اليوم عبر `data-daypart`.
- **إمكانية الوصول**: كل صف صفحة مفردة بزري تحكم مسمّيين، ورقة علوية واحدة بفخ تركيز وEscape وقفل تمرير، أهداف لمس ≥ 44px، تباينات موثقة في `globals.css` (نص 4.5:1+، عناصر 3:1+)، وحركة مقيّدة بـ `prefers-reduced-motion`.
- **الخطوط**: مستضافة ذاتيًا (Fontsource) — IBM Plex Sans Arabic للواجهة (400–700) وAmiri للنص الشرعي (400/700) بلا طلبات خارجية ولا انزياح تخطيط.

## انحرافات عن تقرير التدقيق (متعمّدة)

1. `adhan` لم يُضَف: إصلاح الإزاحة عبر `Intl` (قدرة منصة) يعالج العلّة الفعلية دون تغيير رياضيات مجرَّبة، ويحفظ واجهة `PrayerTimesResult` كما هي.
2. TypeScript يبقى 5.9: واجهة TS 7.0 البرمجية غير كافية لـ typescript-eslint حتى 7.1.
3. Playwright E2E لم يُضَف: بيئة التطوير لا تتيح تنزيل متصفح (شبكة محجوبة)، وشحن اختبارات غير مُتحقَّق منها مخالف لمبادئ التدقيق. البديل المُنفَّذ والمعتمد في CI: اختبار دخان إنتاجي حقيقي (`npm run test:smoke`) يُقلع خادم `next start` ويفحص كل مسار وأصول PWA والنسخة المستقلة. يُستكمل Playwright عند توفر بيئة بمتصفح.

## خارطة الطريق — حالة التنفيذ

انظر `AUDIT_REPORT.md` للخطة الكاملة. **كل المراحل من 0 إلى 10 منجزة:**

| المرحلة | الحالة | ما نُفِّذ |
|---|---|---|
| 0 — الأساس | ✅ | lockfile، README، اختبارات ذهبية (13)، CI كامل (lint + typecheck + test + build + smoke) |
| 1 — التصحيح | ✅ | مخزن دوري المفاتيح، إزاحة IANA، عامل خدمة صادق، إزالة الاعتماديات الميتة، خطوط مستضافة، لا وميض ليلي |
| 2 — نظام «رقّ» | ✅ | رموز `@theme`، بدائيات وصول (Sheet بفخ تركيز)، تباينات موثقة، أيقونات lucide 1.x |
| 3 — المعمارية | ✅ | مسارات حقيقية، Next 16 + Turbopack، موفّر مركزي |
| 4 — اليوم | ✅ | المحطة الحالية أولًا، شريط مواقيت، روابط `?focus=`، عمودان على سطح المكتب |
| 5 — التتبع | ✅ | أسبوع ينجو من منتصف الليل، سجل 30 يومًا، تراجع عبر Toast |
| 6 — المحتوى | ✅ | 61 صفحة قراءة ساكنة، خط للمحتوى الشرعي، بحث معرَّب (Ctrl+K) |
| 7 — الثانوي وPWA | ✅ | أيقونات PNG + apple-touch (أنبوب sharp)، **النسخة المستقلة أعيد بناؤها بـ esbuild** — ملف واحد، خطوط مضمّنة، صفر مراجع خارجية (`npm run offline`) |
| 8 — الحركة | ✅ | خمس حركات مسموحة، `prefers-reduced-motion`، و«فجر» يتتبع محطة اليوم |
| 9 — الأداء والوصول | ✅ | المحتوى والتذييل خادميان، رابط تجاوز، أهداف لمس 44px، CSS كامل 8.4KB مضغوطًا |
| 10 — الاختبار | ✅ | ذهبية (13) + وحدة (21) + دخان إنتاجي (10) — خضراء جميعها في CI |

### مقترحات بعد اكتمال الخطة (اختياري)
- تحويل مقاطع «اليوم/المواسم» إلى Server Components جزئية بعد استقرار المتطلبات التفاعلية.
- Playwright E2E على التدفقات الكاملة عند توفر بيئة بمتصفح.
- بيانات «تقدّمي» التاريخية: مقارنات شهرية وتصدير.

### السكربتات

| الأمر | الوظيفة |
|---|---|
| `npm run icons` | توليد أيقونات PNG من المصدر SVG (sharp) |
| `npm run offline` | بناء النسخة المستقلة (يُستدعى تلقائيًا قبل كل build) |
| `npm test` | الاختبارات الذهبية + الوحدة |
| `npm run test:smoke` | إقلاع خادم الإنتاج وفحص كل المسارات والأصول |
